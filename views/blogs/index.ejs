<% include ../partials/header %>

<h1>index.ejs</h1>
<% blogs.forEach((blog) => { %>
    <div>
        <h3><%= blog.title %></h3>
        <img src="<%= blog.image %>">
        <p><%= blog.body %></p>
    </div>
<% }) %>

<% include ../partials/footer %>